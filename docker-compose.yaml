version: "3.9"
services:
  negotiated-rates-backend:
    build: .
    image: negotiated-rates-backend:v1
    container_name: negotiated-rates-backend
    restart: always
    network_mode: host
#    ports:
#      - "8090:8090"
#    networks:
#      - spring-cloud-network
    volumes:
      - /var/logs:/logs

#networks:
#  spring-cloud-network:
#    driver: bridge